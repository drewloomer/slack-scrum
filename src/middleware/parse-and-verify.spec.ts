describe('Verify Slack Signature Middleware', () => {
  it('should reject replay attacks', () => {
    console.log('@todo');
  });

  it('should reject bad secrets', () => {
    console.log('@todo');
  });
});
